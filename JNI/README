The make file expects libvpx.so to be located in x64/

The following configure parameters are known working:
../libvpx/configure --enable-pic --enable-shared

make
make run
make display #(show single frame using imagemagick)
make play    #(use mplayer to play output)

